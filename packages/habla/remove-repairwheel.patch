--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,11 +1,10 @@
 [build-system]
 requires = [
     "setuptools>=42",
     "wheel",
     "ninja",
     "cmake>=3.12",
-    "repairwheel",
     "setuptools-scm>=8"
 ]
 build-backend = "setuptools.build_meta"
